# Environment variables for vnbwedding (Production)

# --- Database ---
# Tells Prisma to use the PostgreSQL driver.
DB_PROVIDER="postgresql"

# Connection string for the PostgreSQL container.
DATABASE_URL="postgresql://vnbwedding_user:{{ vault_db_password }}@localhost:5432/vnbwedding"

# --- Security ---
# A long, random string used to sign cookies.
# Generate one with: openssl rand -base64 32
COOKIE_SECRET="{{ vault_cookie_secret }}"

# The password to access the admin dashboard.
ADMIN_PASSWORD="{{ vault_admin_password }}"

# --- Server ---
# The port the application will run on.
PORT=3000

# The environment mode.
NODE_ENV=production
